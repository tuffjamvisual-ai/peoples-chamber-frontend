import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Esports',
  context: 'This page includes the questionnaires completed by respondents for the Community and Engagement Survey.',
  positions: [],
};

export default data;
