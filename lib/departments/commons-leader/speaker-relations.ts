import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Speaker Relations',
  context: 'Speaker Relations is a key policy area overseen by the Office of the Leader of the House of Commons.',
  positions: [],
};

export default data;
