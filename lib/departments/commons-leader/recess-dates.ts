import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Recess Dates',
  context: 'Recess Dates is a key policy area overseen by the Office of the Leader of the House of Commons.',
  positions: [],
};

export default data;
