import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Speed Limits',
  context: 'This guidance explains how local transport authorities ( LTAs ) should prepare statutory local transport plans ( LTPs ). It explains the relevant statutory duties and sets out the overall approach LTAs are expected to follow.',
  positions: [],
};

export default data;
