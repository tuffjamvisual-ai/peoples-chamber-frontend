import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Space',
  context: 'This joint statement by representatives from UK universities, states the signatories’ support for the scale of ambition and actions set out in the AI for Science Strategy .',
  positions: [],
};

export default data;
