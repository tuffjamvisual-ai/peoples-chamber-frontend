import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Defence Exports',
  context: 'UKEF publishes details of all spending over £500 using a GPC (departmental debit card) on a monthly basis.',
  positions: [],
};

export default data;
