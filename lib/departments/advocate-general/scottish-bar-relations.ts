import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Scottish Bar Relations',
  context: 'Spend over £25,000 by the Office of the Advocate General for Scotland, updated on a month-by-month basis.',
  positions: [],
};

export default data;
