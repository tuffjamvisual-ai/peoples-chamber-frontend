import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Curriculum',
  context: 'The academies accounts return ( AR ) 2024/25 has closed. The AR 2024/25 will remain available to trusts in read only mode. For further details, read the academies accounts return .',
  positions: [],
};

export default data;
