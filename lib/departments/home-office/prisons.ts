import type { ControlZoneData } from '../types';

const data: ControlZoneData = {
  zone: 'Prisons',
  context: 'The guide contains a glossary of terms, notes on interpreting the figures, and information on sources of data on other police powers.',
  positions: [],
};

export default data;
